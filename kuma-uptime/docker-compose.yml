services:  
  uptime-kuma:
    container_name: server-uptime
    image: louislam/uptime-kuma:1
    restart: unless-stopped
    networks:
    - web
    # ports:
    #   - "3001:3001"
    volumes:
      - ./kuma-uptime:/app/data
